
                        
                 @model Follower         
                           
                <div class="container">
                <div class="row followers">
                    @foreach (Follower item in ViewBag.Followers)
                           {
                    <div class="d-flex justify-content-around mt-3 mb-2 istek">
                        <div class="bir d-flex">
                            <a href="/home/profil?UserNickName=@item.FollowerFromUser.UserNickName">
                                <img src="~/img/@item.FollowerFromUser.UserProfilePhotoUrl" class="pp">
                            </a>
                           <a href="/home/profil?UserNickName=@item.FollowerFromUser.UserNickName" style="color:#017EC4"> <h3 class="nm">@item.FollowerFromUser.UserNickName</h3></a>
                        </div>
                            
                        <div class="iki">
                            <button udi=@item.FollowerFromUserId class="btn btn-primary py-1 px-3 acceptBtnn">Qebul et</button>
                            <button udi=@item.FollowerFromUserId  class="btn btn-secondary py-1 px-2 declineBtn">Sil</button>
                        </div>
                    </div>
                            }

               </div>
            </div>  
            <script src="~/Js/index.js"></script>